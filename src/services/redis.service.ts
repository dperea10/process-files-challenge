// import redis from 'redis'

// const client = redis.createClient();

// const redisPush = ({queue, task}:string) => {
//     client.set(queue, task, (err, reply) => {
//         console.log(reply);
//       });
      
//       client.quit();
// }